#include <stdio.h>

int main() {
    int n;
    printf("Start from (e.g. 5): ");
    if (scanf("%d", &n)!=1) return 0;

    for (int i = n; i >= 1; --i) {
        for (int k = 0; k < i; ++k)
            printf("%d", i);
        putchar('\n');
    }
    return 0;
}
